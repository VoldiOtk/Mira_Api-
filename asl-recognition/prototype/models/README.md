# Models for Prototype

Ce prototype réutilise les modèles existants du projet principal:

- `../model/model_hands.pth` + `../model/model_hands_meta.json` (alphabet)
- `../model/model.pth` + `../model/model_meta.json` (gestes mots)

Vous pouvez ultérieurement remplacer ces fichiers par des versions spécialisées
VoFaLink sans changer l'API du prototype.
